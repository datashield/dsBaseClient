% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ds.isValid.R
\name{ds.isValid}
\alias{ds.isValid}
\title{Checks if an object is valid}
\usage{
ds.isValid(x = NULL, datasources = NULL)
}
\arguments{
\item{x}{a character, the name of a vector, dataframe or matrix.}

\item{datasources}{a list of \code{\link{DSConnection-class}} objects obtained after login. If the <datasources>
the default set of connections will be used: see \link{datashield.connections_default}.}
}
\value{
a boolean, TRUE if input object is valid and FALSE otherwise.
}
\description{
Checks if a vector or table structure has a number of observations equal to or greater
than the threshold set by DataSHIELD.
}
\details{
In DataSHIELD, analyses are possible only on valid objects to ensure the ouput is not disclosive.
This function checks if an input object is valid. A vector is valid if the
number of observations are equal to or greater than a set threshold. A factor vector is valid if all
its levels (categories) have a count equal or greater than the set threshold. A dataframe or a matrix
is valid if the number of rows is equal or greater than the set threshold.
}
\examples{
\dontrun{

  # load that contains the login details
  data(logindata)

  # login and assign specific variable(s)
  myvar <- list("LAB_TSC", "GENDER")
  conns <- datashield.login(logins=logindata,assign=TRUE,variables=myvar)

  # Example 1: Check if the dataframe assigned above is valid
  ds.isValid(x='D')

  # clear the Datashield R sessions and logout
  datashield.logout(conns)

}

}
\author{
Gaye, A.
}
